# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Current State
The repository appears to be uninitialized - no source files, configuration files, or documentation were found in the project directory.

## Next Steps
1. Initialize your project with appropriate files (index.html, CSS/JS directories, etc.)
2. Update this CLAUDE.md file with project-specific information including:
   - Common development commands (build, test, serve)
   - Project architecture overview
   - Any framework-specific workflows

Once your project structure is established, this file should contain concrete commands and architectural context that would require reading multiple files to understand.
## 运行环境
- 当前系统为 Windows 操作系统，终端使用 PowerShell 环境
- 所有终端命令必须兼容 Windows PowerShell / CMD，禁止输出 Linux/macOS 专属命令
- 删除文件夹必须使用 `rd /s /q`，禁止使用 `rm -rf`
- 文件路径优先使用正斜杠 `/`，避免反斜杠转义问题
- 链式命令使用 `;` 或 `&&` 时必须符合 Windows 语法

